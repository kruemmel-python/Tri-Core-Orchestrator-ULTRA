[pytest]
minversion = 8.0
testpaths = tests, .
addopts = -ra
# Streamlit spamt via Logger, nicht immer als Python-Warning.
# Trotzdem: häufige Warnungen global rausfiltern.
filterwarnings =
    ignore:.*deprecated.*:Warning
    ignore:.*distutils.*:Warning
